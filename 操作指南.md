# 操作指南

## 📋 你需要完成的事项清单

### ✅ 已完成（AI已生成）
- [x] 完整的 Vue3 + Vite 前端项目代码
- [x] 所有组件（图表、筛选、布局、根因分析等）
- [x] 示例数据文件（JSON格式）
- [x] GitHub Actions 自动部署配置
- [x] 项目文档

### 🔴 你需要完成的事项

---

## 第一步：安装 Node.js（必需）

### Windows 安装步骤

1. **打开浏览器**，访问：https://nodejs.org

2. **下载 LTS 版本**（长期支持版，推荐）
   - 点击绿色按钮 "20.x.x LTS"
   - 下载 Windows Installer (.msi)

3. **运行安装程序**
   - 双击下载的 .msi 文件
   - 一路点击 "Next"（使用默认设置即可）
   - 点击 "Install" 安装
   - 安装完成后点击 "Finish"

4. **验证安装**
   - 打开命令提示符（Win + R，输入 `cmd`，回车）
   - 输入以下命令：
   ```bash
   node -v
   npm -v
   ```
   - 如果显示版本号（如 v20.x.x），说明安装成功

---

## 第二步：运行项目（本地预览）

### 操作步骤

1. **打开终端**
   - 在项目文件夹上右键
   - 选择 "在终端中打开" 或 "Git Bash Here"

2. **进入项目目录**
   ```bash
   cd "C:/Users/z00958183/Eurekaclaw/Workspace/southern-africa-telecom-dashboard_20260903_0914"
   ```

3. **安装依赖**（首次运行需要）
   ```bash
   npm install
   ```
   - 这个过程需要 1-3 分钟
   - 看到进度条走完即可

4. **启动开发服务器**
   ```bash
   npm run dev
   ```

5. **打开浏览器查看**
   - 终端会显示类似：
   ```
   VITE v5.0.0  ready in 500 ms

   ➜  Local:   http://localhost:5173/
   ➜  Network: use --host to expose
   ```
   - 按住 Ctrl，点击链接打开浏览器
   - 或者手动在浏览器输入：http://localhost:5173

---

## 第三步：部署到 GitHub Pages（可选）

### 前提：创建 GitHub 账号

1. **打开浏览器**，访问：https://github.com

2. **注册账号**
   - 点击右上角 "Sign up"
   - 填写邮箱、密码、用户名
   - 验证邮箱

### 部署步骤

1. **创建新仓库**
   - 登录 GitHub 后，点击右上角 "+" → "New repository"
   - Repository name 填写：`southern-africa-telecom-dashboard`
   - 选择 "Public"（公开）
   - 点击 "Create repository"

2. **初始化 Git 并推送代码**

   在项目目录的终端中执行：

   ```bash
   # 初始化 Git
   git init

   # 添加所有文件
   git add .

   # 提交
   git commit -m "Initial commit: 南部非洲运营商财报看板"

   # 添加远程仓库（替换成你的用户名）
   git remote add origin https://github.com/你的用户名/southern-africa-telecom-dashboard.git

   # 推送代码
   git push -u origin main
   ```

   > 如果提示输入用户名密码，输入你的 GitHub 账号密码

3. **启用 GitHub Pages**
   - 进入仓库页面
   - 点击 "Settings" → 左侧菜单找到 "Pages"
   - Source 选择 "GitHub Actions"
   - 等待自动部署完成（约 1-2 分钟）

4. **访问线上看板**
   - 部署完成后，Pages 页面会显示访问地址：
   ```
   Your site is live at https://你的用户名.github.io/southern-africa-telecom-dashboard/
   ```
   - 点击链接即可访问线上版本

---

## 📁 项目文件位置

| 文件/目录 | 说明 |
|---------|------|
| `package.json` | 项目配置和依赖 |
| `vite.config.js` | Vite 构建配置 |
| `src/` | 源代码目录 |
| `src/components/` | Vue 组件 |
| `src/data/telecom-data.json` | 数据文件（更新数据改这里） |
| `.github/workflows/deploy.yml` | 自动部署配置 |

---

## 🔄 后续更新数据

当你有新的财报数据时：

1. **修改数据文件**
   - 打开 `src/data/telecom-data.json`
   - 更新 `quarterlyData` 数组中的数据
   - 更新 `lastUpdate` 和 `updateTime`

2. **本地预览验证**
   ```bash
   npm run dev
   ```
   - 检查数据是否正确显示

3. **推送更新**
   ```bash
   git add .
   git commit -m "更新财报数据至 2026 Q3"
   git push
   ```
   - GitHub Actions 会自动重新构建部署

---

## ⚠️ 常见问题

### Q1: npm install 报错
**原因**：网络问题或 npm 源访问慢

**解决**：切换到国内镜像
```bash
npm config set registry https://registry.npmmirror.com
npm install
```

### Q2: npm run dev 报错 "vite not found"
**原因**：依赖未安装成功

**解决**：重新安装
```bash
rm -rf node_modules
npm install
```

### Q3: 浏览器打开是空白页
**原因**：可能是浏览器兼容问题

**解决**：使用 Chrome、Edge 或 Firefox 最新版本

### Q4: git push 提示权限错误
**原因**：未登录 GitHub

**解决**：
- 安装 Git Credential Manager
- 或使用 Personal Access Token：
  1. GitHub → Settings → Developer settings → Personal access tokens
  2. 生成 token，勾选 repo 权限
  3. push 时用 token 作为密码

---

## 🎯 快速命令参考

```bash
# 安装依赖
npm install

# 本地开发
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview

# Git 推送
git add .
git commit -m "更新说明"
git push
```

---

## 📞 需要帮助？

如果遇到问题：
1. 检查 Node.js 是否正确安装（`node -v`）
2. 检查依赖是否安装（`npm list`）
3. 查看终端错误信息
4. 重新执行安装步骤

---

**祝你顺利运行项目！** 🎉